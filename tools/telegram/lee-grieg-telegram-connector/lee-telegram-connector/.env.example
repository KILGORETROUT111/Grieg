# ==== LEE ↔ Telegram Connector (.env) ====
# Copy to .env and fill in values before running `docker compose up`

# Telegram Bot token (from @BotFather)
TG_BOT_TOKEN=YOUR_TELEGRAM_BOT_TOKEN_HERE

# Public HTTPS URL for webhook (e.g., from Cloudflare Tunnel / ngrok)
PUBLIC_WEBHOOK_BASE=https://example.com

# Redis connection string
REDIS_URL=redis://redis:6379/0

# Postgres connection string (SQLAlchemy format)
DATABASE_URL=postgresql+psycopg2://postgres:postgres@postgres:5432/lee

# Salt for hashing/redaction (rotate carefully)
AUDIT_SALT=change_me_safely

# CORS (comma-separated origins); set to * for development only
ALLOW_ORIGINS=http://localhost:8000
